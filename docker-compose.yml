services:
  redis:
    image: redis:8.10-alpine
    container_name: redis-server
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --requirepass myRedisPassword123 --appendonly yes
    volumes:
      - redis_data:/data

volumes:
  redis_data: